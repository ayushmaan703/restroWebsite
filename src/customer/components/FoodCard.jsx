import { Plus, Minus } from 'lucide-react';
export default function FoodCard({ item, qty = 0, onAdd, onChange }) { const hasImage = Boolean(item.image); return <article className={`customer-food-card ${hasImage ? '' : 'customer-no-image'}`}>{hasImage ? <><img className="food-image" src={item.image} alt={item.name} onError={e => { e.currentTarget.style.display = 'none'; e.currentTarget.nextElementSibling.style.display = 'grid' }} /><div className="food-placeholder" style={{ display: 'none' }}>No image</div></> : <div className="food-placeholder">No image</div>}<div className="food-info"><div className="food-name">{item.veg && String(item.veg).toLowerCase().includes('non') ? '🔴' : '🟢'} {item.name}</div>{item.description && <div className="food-desc">{item.description}</div>}<div className="food-bottom"><span className="food-price">₹{item.price.toFixed(0)}</span>{qty ? <div className="qty-mini"><button onClick={() => onChange(-1)}><Minus size={14} /></button><b>{qty}</b><button onClick={() => onChange(1)}><Plus size={14} /></button></div> : <button className="add-button" onClick={onAdd}><Plus size={20} /></button>}</div></div></article> }
